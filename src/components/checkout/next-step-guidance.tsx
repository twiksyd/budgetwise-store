export function NextStepGuidance() {
  return (
    <div className="mx-auto max-w-sm text-center">
      <p className="font-heading text-sm font-semibold">Next step</p>
      <p className="text-muted-foreground mt-2 text-sm leading-relaxed">
        Copy the full order message, open Messenger, then paste and send it.
        Wait for a BudgetWise representative to reply with your payment
        instructions.
      </p>
    </div>
  );
}
